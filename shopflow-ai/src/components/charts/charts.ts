/**
 * Chart primitives (placeholder).
 *
 * The analytics/reports modules will add Recharts- or Tremor-style wrappers
 * here (e.g. AreaChart, BarChart, DonutChart) that consume the theme tokens
 * from `globals.css` so light/dark mode works out of the box.
 */
export {};
