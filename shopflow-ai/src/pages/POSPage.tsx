/**
 * Route entry for the Smart POS module.
 * The screen itself lives in the feature folder so the module stays portable.
 */
export { POSPage as default, POSPage } from '@/features/pos/pages/POSPage';