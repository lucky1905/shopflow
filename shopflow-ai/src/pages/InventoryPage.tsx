/**
 * Route entry for the Inventory module.
 * The screen itself lives in the feature folder so the module stays portable.
 */
export { InventoryPage as default, InventoryPage } from '@/features/inventory/pages/InventoryPage';
